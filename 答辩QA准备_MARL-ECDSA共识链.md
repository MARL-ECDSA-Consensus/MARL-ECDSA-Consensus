# 竞赛答辩 Q&A 准备

> MARL-ECDSA 共识链 — 面向多智能体强化学习的区块链 AI 协同共识机制
> 准备时间：2026-07-11

---

## 一、核心论证逻辑（答辩主线）

```
信任困境 → Nash均衡证明(λ≥0.0667合作是优势策略) 
        → 实验验证(total_reward +49.5%) 
        → 安全体系(3攻击100%防御) 
        → 工程完整性(338测试)
```

---

## 二、高频问题与回答

### Q1: 你们这个项目到底解决了什么问题？

**答**：多智能体协作中的信任困境。传统 MARL 中智能体可能采取自私策略，缺乏信任验证和激励机制。我们通过区块链的 ECDSA 身份认证、贡献度加权共识（CW-PBFT）和智能合约激励，让智能体从"个体最优"转向"协作最优"。

### Q2: 为什么用 IQL 不用 QMIX？/ 代码里为什么有 QMIXAgent？

**答**：我们的算法实际是 IQL（独立 Q-learning）。类名 `QMIXAgent` 是历史原因，代码中已使用 `IQLEstimator` 别名反映实际算法。选择 IQL 是因为它作为基线能更干净地展示 BC 激励的效果，避免 QMIX 混合网络的混淆效应。QMIX/VDN 作为可选扩展保留。

### Q3: BC 激励到底提升了多少？为什么有不同口径？

**答**：这取决于评估口径：
- **total_reward（含BC激励）**：BC-MARL 较 Pure 为**正向增益**（定稿 λ=0.1）；竞赛统一以 **env_reward（不含BC激励）公平口径** 申报核心指标（见下）
- **env_reward（不含BC激励，公平口径）**：BC-MARL 提升 **+29.2%**（λ=0.1, 22种子/组, 3000回合收敛；Welch p=0.126 不显著，诚实定位「方向一致、检验力不足」）

total_reward 是主指标，因为智能体实际优化的是 total_reward。env_reward 作为公平性参考，说明 BC 激励不扭曲环境探索行为。

### Q4: 为什么 env_reward 在 bc_marl 下和 pure_marl 差不多？

**答**：这正是 Nash 均衡定理的体现。当 λ=0.1（> λ_min=0.0667）时，BC 奖励足够大，智能体不需要改变环境策略也能获得高 total_reward。env_reward 不变说明 BC 激励**不扭曲环境学习**，这是好的性质——BC 是额外奖励，不是替代环境奖励。

### Q5: λ 取多少？为什么？

**答**：λ=0.1 是定稿配置（λ_min=0.0667，约 1.5 倍安全裕度）。Nash 均衡证明 λ ≥ 0.0667 时合作就是优势策略，λ=0.1 有 50% 的安全裕度（保守假设）。env_reward 公平口径下 BC 提升 +29.2%（Welch p=0.126 不显著，不做因果归因）。

### Q6: 消融实验显示各模块差异不大，为什么？

**答**：消融实验在 200 回合短训练下进行，噪声较大。SecurityGuard 和 CW-PBFT 加权的主要价值不在性能提升，而在**安全保障**——它们不会让训练更快，但能防止攻击（3/3 攻击 100% 拦截）。

### Q7: 这个系统能扩展到多少智能体？

**答**：我们验证了 3 和 5 智能体场景。理论上 CW-PBFT 共识支持到 16 节点（已测试到 40% 拜占庭容错），MARL 部分受限于环境复杂度，扩展到 10+ 智能体只需调整 hidden_dim 和 batch_size。

### Q8: ECDSA 签名会不会太慢？

**答**：实测 ECDSA 签名 0.12ms/次，验证 0.08ms/次。每步签名 3 次，总开销 ~0.36ms，相对 25 步/回合的训练时间可忽略不计（< 0.1%）。

### Q9: 你们的 Merkle 树是真的吗？

**答**：是的。V4.1 版本实现了真实 Merkle 树，`Block.compute_state_root()` 使用交易哈希的两两 SHA256 哈希计算 Merkle 根，替换了早期的占位符。

### Q10: 和现有方案比有什么优势？

**答**：三个核心区别：
1. **Nash 均衡理论保证**：数学上证明合作是优势策略，不是经验调参
2. **ECDSA 安全体系**：FIPS 186-5 标准，3 种攻击 100% 防御
3. **工程完整性**：338 测试，端到端流水线，完整交付文档

---

## 三、技术细节追问

### Q11: CW-PBFT 和标准 PBFT 的区别？

**答**：标准 PBFT 按节点数计票（1 节点 = 1 票），CW-PBFT 按贡献权重计票（贡献高 = 票数高）。在 33% 拜占庭极限下，CW-PBFT 成功率 99% vs 标准 PBFT 79%。

### Q12: SecurityGuard 检测哪些攻击？

**答**：三种：1）k 值重用攻击（相同 r 值→私钥可推导）；2）重放攻击（nonce 不递增）；3）时间戳攻击（±30s 窗口）。

### Q13: 双向协同具体怎么实现？

**答**：BC→MARL：`total_reward = env_reward + λ·bc_score`，bc_score 在回合结束时由激励合约计算。MARL→BC：智能体的行为数据（ECDSA 签名 + 动作）作为交易上链，贡献度评估依据。

---

## 四、防守策略

### 如果被问到不确定的问题

1. **诚实回答**："这个问题我们目前没有深入研究，但我们的架构设计预留了扩展性"
2. **引导到优势领域**："不过我可以说说我们在 Nash 均衡证明上的工作..."
3. **承认局限性**："目前只验证了 3-5 智能体，更多智能体是未来工作"

### 避免的表述

- ❌ 不要夸大实验效果（如声称"40% 提升"而不说明口径）
- ❌ 不要贬低现有方案
- ❌ 不要回避问题（如 CARS 负效果）