# =============================================================================
# MARL-ECDSA 共识链 依赖清单
# =============================================================================

# --- 核心密码学（必需）---
cryptography>=41.0.0

# --- MARL 深度学习（推荐，支持GPU加速）---
# 安装命令: pip install torch
# CPU版: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# --- 数值计算 ---
numpy>=1.24.0
scipy>=1.11.0               # Welch's t-test 统计检验

# --- 可视化（可选）---
matplotlib>=3.7.0          # 结果图表导出
flask>=3.0.0                # Web Dashboard
tqdm>=4.65.0                # 训练进度条

# =============================================================================
# 安装命令
# =============================================================================
# 最小安装（仅密码学+环境，NumPy回退）:
#   pip install cryptography numpy
#
# 完整安装（含训练+可视化）:
#   pip install cryptography torch numpy scipy tqdm matplotlib flask
#
# 纯验证（无PyTorch，NumPy回退）:
#   python main.py --mode bc_marl --demo
# =============================================================================
